export enum FieldType {
  'OBJECT' = 'object',
  'ARRAY' = 'array',
  'VALUE' = 'value',
}

export enum ValueType {
  'ID - UUIDV4' = 'id.uuidv4',
  'Person - Name' = 'person.name',
  'Web - Email' = 'web.email',
  'Text - Word' = 'text.word',
  'Text - Sentence' = 'text.sentence',
  'Time - Date' = 'time.date',
}
